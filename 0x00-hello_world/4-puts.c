#include <stdio.h>
/**
 * main - a program that uses the puts function instead of the printf
 * Return: 0 (Success)
 */
int main(void)
{
	puts("Programming is like building a multilingual puzzle");
	return (0);
}
